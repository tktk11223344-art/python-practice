
'''
#list바꿀 수 있는 묶음
numbers = [10, 20, 30]


numbers[0] = 100  #0번 자리에 있는 값을 100으로 바꿔라
print(numbers)    #전체 출력
print(numbers[0]) #0번자리 출력


numbers.append(40)#추가
print(numbers)    #전체 출력






#tuple바꿀 수 없는 묶음
numbers = (10, 20, 30)
print(numbers[0]) #특정값가져오기
len(numbers)      #개수확인

for n in numbers: #for반복
    print(n) 






business_days = ('월', '화', '수', '목', '금')

print(business_days[3])



#dict 이름표(key) : 내용(value)
person = {"name": "정아","age": 23,"major": "중국학"}
print(person["name"])
print(person["age"])
print(person["major"])


#list는 ~몇번째 자리로 기억 하지만 dict은 key로 기억

person = {"name": "정아","age": 23,"major": "중국학"}
person["age"]=24     #[] 이거 만을 출력해라    
print(person['age']) #[] 이거 만을 변경해라
print(person)


person["university"] = "국민대학교" #[] 이거 만을 추가해라
print(person["university"])

del person['age']    #[] 이거 만을 삭해라
print(person)



person = {"name": "정아",
          "age": 23,
          "major": "중국학"}

for key in person:
    print(key)

for key in person:
    print(person[key])




transaction = {
    "country": "중국",
    "amount": 150000,
    "currency": "CNY",
    "status": "성공"
}
print(transaction['country'])
print(transaction['amount'])
print(transaction['currency'])
print(transaction['status'])



# 문제: 학생 정보 출력하기
# person 딕셔너리를 만든다
# name → "민수"
# age → 22
# major → "경영학"
# for문을 사용해서 value만 하나씩 출력한다

person={'name':'민수','age':'22','major':'경영학'}
for key in person:
    print(person[key])




# 문제: 거래금액 확인하기
# transaction 딕셔너리를 만든다
# country → "한국"
# amount → 150000
# status → "성공"
# "amount"의 값을 출력한다
# "status"의 값을 출력한다
    

transaction={'country':'한국',
             'amount':'150000',
             'status':'성공' }

print(transaction['amount'])
print(transaction['status'])




# 문제: 거래 데이터에서 100,000원 이상인지 확인하기
# transaction 딕셔너리를 만든다
# country → "중국"
# amount → 150000
# status → "성공"
#
# for문으로 transaction의 key를 하나씩 가져온다
# key가 "amount"라면
# 거래금액이 100,000원 이상인지 확인한다
# 100,000원 이상이면 "고액 거래"라고 출력한다





transaction={'country':'중국',
             'amount':150000,
             'status':'성공'}

for key in transaction:
    if key=='amount'and transaction[key]>=100000:
        print('고액거래')



person = {
    'name': 'Minsu',
    'age': 22,
    'major': 'Business'
}

for key in person:
    print(person[key]) #오른쪽 다 출력

print(person['name']) #오른쪽 출력





transaction = {
    'country': 'Korea',
    'amount': 150000,
    'status': 'Success'
}

print(transaction['country'])
print(transaction['amount'])





person = {
    'name': 'Minsu',
    'age': 22,
    'major': 'Business'
}

for key in person:
    print(key)          #name, age, major 가 나오게
    
for key in person:
    print(person[key])  #민수, 22, business 가 나오게




transaction = {
    'country': 'China',
    'amount': 150000,
    'status': 'Success'
}

for key in transaction:
    if key=='amount' and transaction[key]>=100000:
        print('high transaction')


'''



transaction = {
    'country': 'Japan',
    'amount': 80000,
    'status': 'Success'
}


for key in transaction:
    if key=='amount' and transaction[key]>=10000:
        print('high transaction')
